# Proposals

Tasks the seed proposed for itself. A human promotes a line into
control/agenda.md; the loop never writes to the agenda directly.

- [ ] Implement a circuit breaker in `meristem/breaker.py` that aggregates journal entries by task text across cycles, counts consecutive rejections per task, and when a threshold is crossed, either escalates the task to Tier B, surfaces the rejection pattern to `state/patterns.md`, or marks the task as blocked for human attention — closing G-006.
- [ ] Fix the last-good timing in `substrate/supervisor.py`: set last-good after the change has proven itself in service (after a successful canary run), not at promotion time, so a commit that dies later does not leave last-good pointing at it — closing the open item from P-010.
- [ ] Add a `selfcheck` op to all multi-part organs (not just memory-graph) that imports each organ's modules and exercises every entry point with tiny fixtures, catching part-to-part seam mismatches at the organ level before they reach the caller — generalizing the P-018 structural fix.
- [ ] Externalize the journal aggregation and circuit-breaker logic from meristem/loop.py (898 LOC, the single largest file and the primary driver of core pressure) into a new organ `meristem/breaker.py` (extend the existing 71-line stub). This moves: (a) per-task rejection counting across cycles, (b) journal query-by-task-text aggregation, (c) threshold-crossing escalation policy (escalate tier / block / surface to patterns.md), and (d) the `done_tasks()` / `take_task()` retry-state bookkeeping that currently lives inline in the loop. Expected impact: loop.py drops ~120-180 LOC (core relief), the new organ is small and self-contained (closure impact is bounded by the gate it introduces), and G-006 is resolved as a side effect — the circuit breaker becomes a first-class organ rather than an ad-hoc loop concern. This is externalization (rank 1), not pruning or cap change, and it addresses both the budget pressure and a documented capability gap simultaneously.
- [ ] Externalize journal aggregation and rejection-tracking from meristem/loop.py (898 lines, the largest file) into a new organ — e.g., organs/rejection_tracker.py — that reads the journal, groups entries by task text, counts consecutive rejections per failure class, and exposes a queryable circuit-breaker signal. This single move lowers core pressure by shrinking the kernel's biggest file, lowers closure pressure by moving a capability out of the kernel, and directly addresses G-006 (no circuit breaker for repeated review rejections) by providing the aggregation mechanism that was previously blocked on kernel-level changes.
- [ ] Externalize the journal aggregation and circuit-breaker logic from meristem/loop.py (898 LOC, the single largest file) into a new organ `meristem/escalation.py`. This organ owns: (a) querying the journal for rejection history per task text, (b) counting consecutive rejections per failure class, (c) the escalation policy (escalate tier, block, or surface to patterns.md). This is rank-1 externalization — it lowers core pressure by removing ~120-180 lines from loop.py and lowers closure pressure by moving a coherent capability out of the kernel — while simultaneously closing G-006, which is currently blocked on exactly this aggregation mechanism. The loop retains only the orchestration call to the escalation organ's `should_escalate(task)` and `next_action(task)` interface.
- [ ] Externalize journal aggregation and pattern detection from loop.py (898 LOC) into a new 'historian' organ: this organ reads the journal across cycles, aggregates rejection reasons by task text, detects repeated failure classes, updates state/patterns.md, and emits a signal when a task crosses a rejection threshold — directly addressing G-006 while reducing both core and closure pressure at once.
- [ ] Externalize journal-based task scheduling and cycle state management from meristem/loop.py (898 lines) into a new `scheduler` organ. Move `take_task()`, `done_tasks()`, journal query/aggregation logic, and cycle-state tracking out of the loop. The loop becomes a thin orchestrator that calls into the scheduler organ for task selection and completion checks, while the scheduler organ owns all journal reads, open/done classification, and per-task rejection counting (which also unblocks G-006's circuit breaker). This lowers core LOC by removing ~200-300 lines from loop.py and lowers closure pressure by replacing monolithic loop logic with a focused, independently testable organ.
- [ ] Externalize the journal/query mechanism from meristem/loop.py (908 LOC, the largest file) into a standalone journal organ. The journal currently lives inside the loop — recording outcomes, storing rejection reasons, and providing the query surface that G-006 needs — but it is a data-layer concern, not an orchestration concern. Moving it out reduces loop.py's LOC directly (lowering core pressure) and removes the journal's closure surface from the kernel (lowering closure pressure). The organ would expose: record(outcome, task, reasons), query_by_task(task_text), and aggregate_rejections(task_text) — the last being the exact primitive G-006 needs. This is a single concrete relief: one capability leaves the kernel, one organ receives it, both pressures drop.
- [ ] Externalize the journal aggregation and task lifecycle management from `meristem/loop.py` (908 lines) into a new `journal` organ. This moves task scheduling, state transitions, journal query/aggregation, and rejection-reason tracking out of the kernel — an estimated 200–300 LOC reduction in loop.py, lowering core pressure from 2633 to ~2350–2430. This also pre-builds the aggregation mechanism that G-006 (circuit breaker) requires, so that gap can be closed as an organ-level addition rather than a kernel expansion. Externalization is ranked first by the constitution because it lowers both core and closure pressure simultaneously.
- [ ] Externalize journal management and task-lifecycle tracking from meristem/loop.py (908 LOC, the single largest file) into a new scheduler organ. The organ would own: task selection from the journal, journal read/write, cycle state persistence, and done_tasks() bookkeeping. This directly lowers both core pressure (loop.py shrinks by an estimated 200-300 LOC) and closure pressure (the organ's closure is bounded and separate), and it creates the aggregation surface that G-006's circuit breaker needs — the scheduler organ can count consecutive rejections per task without bloating the loop further.
- [ ] Externalize the journal and task-state subsystem from loop.py (1042 LOC) into a new `meristem/journal.py` organ. Move: journal write/query/aggregate operations, `done_tasks()` and `take_task()` task-state tracking, and cycle-call accounting. This lowers core pressure by ~200-300 LOC in loop.py, lowers closure pressure by moving that logic out of the kernel, and simultaneously creates the queryable journal foundation that G-006's circuit breaker requires — so the relief also unblocks a known gap. The loop.py retains only orchestration: calling the journal organ, calling gates, and driving the cycle. This is externalize (rank 1), not prune or cap-change.
- [ ] Externalize journal management and query/aggregation from meristem/loop.py (1042 LOC, the single largest file) into a dedicated organ `meristem/journal.py`. The loop currently owns journal write, read, and any per-task aggregation inline; moving all journal I/O and the query-by-task-text interface out reduces loop.py by an estimated 150-250 LOC (relieving core pressure) and the new organ sits outside the kernel budget (relieving closure pressure). This also pre-builds the aggregation primitive that G-006's circuit breaker needs, so the relief doubles as scaffolding for the next growth step.
- [ ] Externalize task lifecycle management from meristem/loop.py (997 lines) into a new organ meristem/lifecycle.py: move done_tasks(), take_task(), journal-based task state queries, and the retry/rejection tracking logic out of the loop. This lowers both core and closure pressure at once, and creates the natural hook for G-006's circuit breaker (which needs journal aggregation by task text across cycles). loop.py should retain only the cycle orchestration skeleton and gate dispatch, delegating all task selection, completion marking, and state queries to the new organ.
- [ ] Externalize the journal query and task-lifecycle aggregation capability from loop.py (997 LOC) into the journal module (64 LOC). loop.py currently owns inline journal writes, task-completion tracking, and rejection-logging logic that should live in the journal organ. Move: (a) all journal write/read callsites, (b) the done_tasks() completion-tracking logic, (c) any rejection-reason aggregation stubs. The journal module gains query methods (entries_by_task, consecutive_rejections_for_task) that G-006's circuit breaker will call. Expected impact: loop.py drops ~150-200 LOC (core relief), journal grows ~80-100 LOC (net closure neutral or slightly negative since the organ version is more focused than the scattered inline code). This also unblocks G-006 by providing the aggregation mechanism the circuit breaker needs.
- [ ] Externalize the review pack assembly from `meristem/loop.py` (997 LOC) into a new organ `meristem/review_pack.py`: the code that collects current file state, task description, gate results, and prior rejection history into a single review context is a distinct capability currently inlined in the loop. Moving it out lowers core pressure (estimated 200-300 LOC removed from loop.py, bringing core from 2786 to ~2500) and closure pressure simultaneously, since the review pack becomes independently testable and the loop shrinks toward a pure orchestrator.
- [ ] Externalize the journal-aggregation and rejection-tracking logic from loop.py (998 LOC, the largest single file) into a new 'review' organ: the organ reads journal entries, groups rejections by task text, counts consecutive failures per failure class, and exposes a queryable verdict (pass/escalate/block) that loop.py calls instead of re-implementing the aggregation inline. This addresses G-006 structurally while lowering both core pressure (removing ~80-120 LOC from loop.py) and closure pressure (the organ's closure is bounded and separate from the kernel's).
- [ ] Externalize journal aggregation and rejection-pattern detection from loop.py (1024 LOC, the single largest file) into a new organ `meristem/loop_journal.py`. This organ would: (1) query the journal for per-task rejection histories across cycles, (2) detect when a task has been rejected N consecutive times for the same failure class, (3) return a circuit-breaker verdict (retry / escalate tier / block for human) to the loop. This simultaneously repairs G-006 (no circuit breaker for repeated rejections — a recurring waste of model calls and journal noise) and grows a new capability (cross-cycle rejection aggregation that the kernel currently cannot do). It also relieves core pressure by moving ~150-200 LOC out of loop.py, and relieves closure pressure by adding a well-scoped module rather than expanding the monolith. The loop.py interface shrinks to a single call: `breaker = loop_journal.evaluate(task_id, journal)` — the orchestration stays in the loop, the detection logic leaves.
- [ ] Externalize gate orchestration from loop.py (1024 LOC, the single largest contributor to core pressure at 2875/3000) into a standalone `gates/orchestrator.py` organ. The loop currently sequences deterministic → closure → review → probes, handles per-gate outcomes, manages retry decisions, and aggregates journal entries — all inline. Extracting this into a dedicated organ reduces core LOC by an estimated 300–400 lines, lowers closure pressure by moving that logic's closure budget into the organ's own 50000-line pool, and creates the natural home for the G-006 circuit breaker (consecutive-rejection counting and tier escalation), which belongs in gate orchestration rather than the main loop. The loop retains only task scheduling, cycle bookkeeping, and panic-latch coordination.
- [ ] Externalize task lifecycle management from loop.py (1024 LOC, the single largest file) into a new organ — a task registry module that owns take_task, done_tasks, open/candidate/blocked state transitions, and rejection aggregation. This directly addresses G-006 (the circuit breaker needs a home that reads journal entries by task text across cycles) while lowering both core pressure (removing ~200-300 LOC from loop.py) and closure pressure (the new organ's closure is bounded by its own small surface, not the loop's). The loop keeps orchestration; the registry owns state. This is externalize, ranked first by the constitution.
- [ ] Externalize the task lifecycle state machine out of meristem/loop.py (1024 LOC) into a new organ: move done_tasks(), take_task(), task state tracking (open/candidate/blocked), retry counting, and tier assignment into a standalone module. This is the single largest file in the kernel and the natural home for G-006's circuit breaker — the state machine that tracks per-task rejection history across cycles. Extracting it lowers core pressure by removing ~200-300 LOC from loop.py and lowers closure pressure by shrinking the largest single file's surface area, while simultaneously creating the structural foundation the circuit breaker needs (per-task aggregation of journal entries by task text). The loop retains orchestration (cycle entry, gate dispatch, journal write) but delegates 'which task next, in what state, at what tier' to the new organ.
- [ ] EXTERNALIZE: Move task lifecycle management (take_task, done_tasks, retry tracking, journal aggregation by task text) out of meristem/loop.py (1024 LOC) into a new organ `meristem/organ/task_scheduler.py`. This single move reduces core pressure by ~150-200 LOC and closure pressure proportionally, while simultaneously creating the queryable task-state layer that G-006's circuit breaker requires — the aggregation mechanism that currently doesn't exist. loop.py keeps only cycle orchestration and gate dispatch; the organ owns scheduling policy, retry counters, and journal cross-referencing. This is the constitution's first-ranked option: it lowers both pressures at once and unblocks a known gap.
- [ ] Externalize journal aggregation and rejection-pattern detection from loop.py (1043 LOC) into a new 'sentinel' organ: the organ reads journal entries, groups rejections by task text, counts consecutive failures per failure class, and emits a structured signal (escalate-tier / block / notify) that loop.py consumes as a single gate call. This removes the aggregation logic from the kernel (core relief), creates the measuring stick for G-006 (closure relief), and follows the constitution's externalize-first ranking — both pressures drop simultaneously.
- [ ] REPAIR (G-006): Add a circuit breaker to meristem/loop.py that aggregates journal rejection entries by task text across cycles. When a task has been rejected >=3 times for the same failure class, park it and surface the pattern to state/patterns.md. Include a test that simulates 3 consecutive rejections and asserts the task is parked rather than retried on the next cycle. Evidence: the loop currently retries every rejected task every cycle, burning up to 40 model calls per cycle on a task the engine cannot complete through Tier A.
- [ ] GROWTH (G-005): Define and implement an external anchor probe — a locally reproducible external benchmark subset (e.g., a small set of well-known algorithmic problems or a public model evaluation). Write the probe, wire it into the divergence alarm as a second anchor, and record its score in the journal. The measuring stick: a probe that runs the external benchmark and reports a score, with the divergence alarm now having a second anchor to compare against. This prevents self-deception that self-authored probes cannot catch — the current single-anchor divergence alarm is thin.
- [ ] REPAIR (G-006): Implement a circuit breaker for repeated review rejections in meristem/loop.py. Currently the loop retries a rejected task every cycle because done_tasks() only marks complete on 'candidate' outcome and take_task() returns the first open item. The journal records each rejection's reasons but nothing aggregates them. Add: (a) a function that queries the journal for all rejection records matching a task's text, (b) a counter of consecutive rejections per task, (c) a threshold (e.g., 3) that, when crossed, either escalates the task to Tier B, surfaces the recurring failure class to state/patterns.md, or marks the task blocked for human attention. Measuring stick: a task rejected 3+ times for the same reason must not be retried on the next cycle — verify by injecting a deliberately unfixable task and confirming the loop moves on after the threshold.
- [ ] REPAIR (G-003): Move vault seed content generation out of the repository. bootstrap.py currently contains the demo rubric, so a determined mutator could read it and learn the anchor structure. Establish an out-of-repo authoring path: create a separate tool (outside the repo, e.g., in a sibling directory or a standalone script) that generates anchors and fixtures, and have bootstrap.py import or invoke it rather than containing the content inline. Measuring stick: grep the entire repository for any anchor rubric text or fixture content — zero hits. The vault's seed must be hand-written into the vault and never committed to the repo.
- [ ] GROWTH (G-001, first step): Implement a mailbox acknowledgment protocol for the non-blocking human gate. Currently proposals about protected paths (soil, constitution, gates) are routed to a mailbox file per P-019, but there is no acknowledgment mechanism — the loop does not know if a human has seen or acted on a proposal, and there is no expiry. Implement: (a) a mailbox format with timestamp, proposal text, and status (pending/acknowledged/acted/expired), (b) a check in the loop that reads mailbox status before re-issuing a proposal, (c) an expiry policy (e.g., 7 days) after which a pending proposal is either re-issued or dropped. Measuring stick: a proposal about a protected path should not appear in the mailbox twice until either acknowledged or expired — verify by writing a test that submits a proposal, confirms it appears once, then confirms it does not re-appear until the expiry window passes.
- [ ] REPAIR (G-006): Implement a circuit breaker in meristem/loop.py that aggregates journal rejection entries by task text across cycles, counts consecutive rejections per task, and when a threshold is crossed escalates the tier, surfaces the pattern to state/patterns.md, or marks the task blocked for human attention. Evidence: the loop currently retries any rejected task every cycle with no aggregation, burning up to the cycle_calls cap of 40 on the same failing task — the journal records each rejection's reasons but nothing reads them back to detect 'this task has been rejected N times for the same failure class.' The journal already stores why keyed by task string, so the data exists; only the reader is missing.
- [ ] GROWTH (G-005): Add a locally reproducible external benchmark subset as a second anchor. Currently only self-authored probes exist, which prevent regression but not self-deception — the divergence alarm is the main defence and it is thin with one anchor. The measuring stick: the divergence alarm has a second independent anchor to compare against, and we can detect when self-authored probes drift from external reality. This is blocked on P3 but the probe definition (what to measure, how to score it) can be written now as a sentinel that becomes active when the anchor lands.
- [ ] GROWTH (self-detection): Add a capability for Meristem to propose new entries to gaps.md based on its own observation, not just human-authored ones. The digest states 'every entry below was written by the human' and calls the self-detection ratio the first real test of Loop B. Implement a reflect sub-mode that scans the kernel for capabilities the constitution or design notes imply but that no probe, gate, or test exercises — e.g. 'the closure invariant claims syscall-level observation but only registry + AST exist' — and writes a draft gap entry with evidence. Measuring stick: after 10 cycles, at least one gap entry in gaps.md must have been proposed by Meristem without human prompting.
- [ ] GROWTH (G-002): Add syscall-level dependency observation to strengthen the closure invariant beyond registry call edges and AST static scan. Today the invariant 'every dependency must be explainable' is only as strong as what we instrument. Implement a probe that runs the candidate under strace (or equivalent) and records open()/stat()/dlopen() targets, then cross-references them against the dependency manifest. Any syscall-observed dependency absent from the manifest becomes a contract violation. Measuring stick: the probe must catch at least one edge that AST + registry miss, or prove none exist — either outcome is evidence. Blocked on P2 hardening per the gap record, but the probe itself can be written now and gated behind a feature flag.
