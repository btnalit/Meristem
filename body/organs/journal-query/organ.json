{
  "id": "journal-query",
  "version": "1",
  "capability": "Cross-cycle task aggregation: groups journal entries by task text and returns counts, rejection reasons, and timestamps, enabling the circuit breaker to detect repeated rejection patterns.",
  "entrypoint": ["python3", "main.py"],
  "input_schema": {"op": "string", "journal_path": "string", "task": "string?"},
  "output_schema": {"ok": "boolean", "tasks": "array"},
  "dependencies": [],
  "probes": ["probe-journal-query-basic"],
  "metrics": ["usage", "success_rate"],
  "lifecycle": "active"
}
