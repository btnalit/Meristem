{
  "id": "rubric-author",
  "version": "1",
  "capability": "Write probe rubrics directly into the vault so no scoring logic resides in the repository.",
  "entrypoint": ["python", "main.py"],
  "input_schema": {"op": "string", "probe_id": "string", "rubric_code": "string", "vault_path": "string"},
  "output_schema": {"ok": "boolean", "path": "string", "error": "string"},
  "dependencies": [],
  "probes": ["probe-rubric-author-vault"],
  "metrics": ["usage", "success_rate"],
  "lifecycle": "candidate"
}
