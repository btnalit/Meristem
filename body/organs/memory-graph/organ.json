{
  "id": "memory-graph",
  "version": "1",
  "capability": "A decaying knowledge graph over Meristem's own records: extracts nodes from state/ registers and the journal, derives typed edges between them, and ranks knowledge by recency so stale, unreinforced entries fade without being deleted.",
  "entrypoint": ["python3", "main.py"],
  "input_schema": {
    "op": "string",
    "args": "object"
  },
  "output_schema": {
    "ok": "boolean",
    "result": "object"
  },
  "dependencies": [],
  "probes": ["probe-memory-graph-basic"],
  "metrics": ["usage", "node_count", "query_latency_ms"],
  "lifecycle": "candidate"
}